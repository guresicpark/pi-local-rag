# File-scheme devDependencies (the vendored type shims under vendor/) are
# copied into node_modules instead of symlinked, so npm installs their
# dependency trees inside node_modules and never writes into vendor/.
install-links=true
